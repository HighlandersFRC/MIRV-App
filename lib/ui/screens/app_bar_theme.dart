import 'package:flutter/material.dart';

class AppBarColor {
  static Color shadowColor = Color.fromRGBO(0, 0, 0, 0);
  static Color backgroundColor = Color.fromRGBO(0, 0, 0, 0);
  static Color foregroundColor = Colors.black;
}
